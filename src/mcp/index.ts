/**
 * @module @etagents/sdk/mcp
 *
 * MCP protocol client and server. Internal to the kernel — users configure, not implement.
 */

export { McpClient } from "./client/client.js";
export { McpServer } from "./server/server.js";
